<Net NetName="USTCsoftware">
    <Blocks>
    {% if node_list %}
        {% for node in node_list %}
            <Block id="{{ node.id }}">
                <id>{{ node.id }}</id>
                <x>{{ node.x }}</x>
                <y>{{ node.y }}</y>
                <width>{{ node.width }}</width>
                <height>{{ node.height }}</height>
                <minWidth>{{ node.minWidth }}</minWidth>
                <Biotype>{{ node.biotype }}</Biotype>
                <Father>{{ node.father }}</Father>
            </Block>
        {% endfor %}
    {% endif %}
    </Blocks>
</Net>